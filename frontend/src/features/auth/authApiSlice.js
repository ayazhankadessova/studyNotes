import { apiSlice } from '../../app/api/apiSlice'
// log out Action Creator
import { logOut } from './authSlice'

export const authApiSlice = apiSlice.injectEndpoints({
    // onject inside of the builder where we can put each endpoint
    // auth post
    // auth logout 
    // auth refresh
    endpoints: builder = 
})
